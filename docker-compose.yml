services:
  backend:
    build:
      context: ./server
    environment:
      PORT: 3001
      CORS_ORIGIN: http://localhost
    restart: unless-stopped

  frontend:
    build:
      context: .
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
